[defaults]
inventory = inventory/hosts.yml
roles_path = roles
retry_files_enabled = False
# Fresh VPS instances get reinstalled/rebuilt; their host keys change often.
host_key_checking = False
interpreter_python = auto_silent

[privilege_escalation]
become = True
become_method = sudo
